# 자비스 설계서 v2 (Fable 5, 2026-07-10) — v1 감리·수정본

전제: `jarvis_full_mockup_v2.html`과 한 세트. v1 파일 보존. 코드·DB·Git 무수정.
기준: reports 15 / items 70 / outcomes 0 / report 1~9 보존 / 최근 커밋 d01e5ce.

---

## C. v1에서 발견한 모순 목록

1. 4단계 구조가 원래 목표(①기록 ②판단 ③행동 ④복기)와 다르게 "저장 결과"를 ②단계로 승격 — 원래 구조와 충돌.
2. 저장 실행 버튼이 한국장에 2개(종목 상세 내 + 확인 블록) 존재 — 단일화 원칙 위반.
3. 미국장에 리스크 입력·저장 전 확인·최종 저장·오류 차단이 누락 — 한국장과 저장 원칙 불일치.
4. 저장 결과에 "수정 모드"가 있으나 입력은 ③에서만 한다는 설계와 모순.
5. ③에 "복기 완료 체크"가 있어 ④복기 단계와 역할 중복.
6. 설계서가 actual_avg_price 등 일부 필드를 "존재 확인"으로 단정 — 실제로는 코드 간접 확인 수준.
7. 수수료 0.25%를 확정값처럼 목업에 표기.
8. R수익률의 entry 기준(계획가 vs 실매수가) 미확정.
9. 미국장 날짜 개념(saved_at/market_date/data_as_of/session) 미분리.
10. 스테퍼가 한국장에만 있고 미국장에 없음 + 상단 메뉴와 스테퍼 구조 불일치.

## D. v2 수정 내용 (→ E. 최종 4단계 구조)

**최종 구조 확정 (권장 방향 채택):**
```
① 오늘 기록  : 시장 자동확인·주가 채우기·테마 참고판·미리보기 생성
② 종목 판단  : 점수·판정·근거·리스크 입력 → 저장 전 확인 → 최종 저장 (버튼 1개)
③ 실제 행동·거래 종료 : 매수/보류/제외 → 체결·청산 입력 → 실현 계산
④ 복기·통계 : 복기 대기 목록·복기 완료 체크·KPI·성과 4영역
[보조 조회] 저장 결과 — 단계 아님, 상단 우측 메뉴, 스테퍼 제외
[보조 기능] 테마 상세·캘린더·지난기록·수동입력·붙여넣기·CSV·사용법·설정
```
사용자 하루 동선: ①→②(저장)→(체결 시)③→(청산 후)④ / 저장 결과는 언제든 조회.
v1 구조를 버린 이유: "종목 판단"은 자비스의 핵심 행위라 독립 단계가 맞고, 저장 결과는 행위가 아닌 조회라 단계 자격이 없음.

수정 반영: 상단 메뉴 = [①·② 작업] [③] [④] + 우측 보조 2개. 시장 전환은 ①·② 화면 내부 KR/US 토글(선택 근거: 두 시장이 같은 4단계를 공유하므로 메뉴 분리 시 스테퍼가 이중화됨). 스테퍼는 KR/US 공통, 1·2단계는 화면 내 앵커 이동. 저장 버튼: 시장별 "이 내용으로 최종 저장" 1개만, 종목 상세의 버튼은 "▼ 저장 전 확인으로 이동"으로 역할 변경. 미국장에 리스크 3칸·저장 전 확인 표(차단 사유 열 포함)·최종 저장·성공 후 이동 추가. 저장 결과 = A안(완전 읽기 전용 + "③에서 입력" 버튼, 수정 모드 삭제). ③의 복기 완료 체크 삭제 → 복기 메모 초안만, ④에 "복기 대기 목록 + 완료 체크" 신설(보류·제외 종목의 '놓친 복기'도 ④에서). 채택 근거(8절 A안): 청산 직후의 복기는 감정 개입으로 품질이 낮고, ④ 주간 리뷰 루틴과 일치.

## F. 실제 행동·거래 상태 판정표 (한 함수 `derive_item_status()`에 집중, 상태 컬럼 미저장)

| actual_action | avg_price | buy_date | qty | exit_price | exit_date | review_done | 화면 상태 | 허용 | 문구 |
|---|---|---|---|---|---|---|---|---|---|
| NULL | - | - | - | - | - | - | 판단만 저장 / 실제 행동 미입력 | 허용 | "③에서 입력" |
| 보류 | 전부 NULL | | | | | 0/1 | 보류 확정 (+복기 완료) | 허용 | - |
| 제외 | 전부 NULL | | | | | 0/1 | 제외 확정 (+복기 완료) | 허용 | - |
| 매수 | 有 | 有 | 有 | NULL | NULL | 0 | 매수 진행 중 | 허용 | - |
| 매수 | 有 | 有 | 有 | 有 | 有(≥매수일) | 0 | 청산 완료 (복기 대기) | 허용 | ④ 대기 목록 표시 |
| 매수 | 有 | 有 | 有 | 有 | 有 | 1 | 복기 완료 | 허용 | - |
| 보류/제외 | - | - | - | **有** | - | - | ⚠ 데이터 불일치 | **저장 차단** | "행동이 보류/제외인데 청산값 존재 — ③에서 행동 재선택" |
| 매수 | 有 | **NULL** | - | - | - | - | (저장 시점 차단) | **차단** | "매수 선택 시 매수일 필수" |
| 매수 | 有 | 有 | **NULL** | 有 | 有 | - | 청산 완료(손익 미계산) | 경고 허용 | "수량 없음 — 실현손익·수익률 미계산, R만 계산" |
| 매수 | 有 | 有 | 有 | 有 | **<매수일** | - | (저장 차단) | **차단** | "매도일이 매수일보다 빠름" |
| 매수 | 有 | 有 | 有 | NULL | NULL | **1** | ⚠ 데이터 불일치 | **차단** | "미청산 종목은 ④ 복기 완료 불가 (보류·제외는 가능)" |

원칙: 신규 저장은 차단, 기존 데이터의 모순은 ⚠ 배지로 표시만(자동 수정 금지) + ③에서 정정 유도.

## G. DB 필드 확인 수준 3단계

| 필드 | 수준 | 근거 |
|---|---|---|
| actual_exit_price / actual_exit_date | **A 코드 확인** | _compute_result_r·SELECT문에서 확인 |
| exit_reason / result_r / plan_followed / filter_ignored | **A 코드 확인** | SELECT·session 처리부 확인 |
| actual_avg_price / actual_buy_date | **B 스키마 확인 필요** | UI("실제 평균 체결가/매수일")와 _fmt_actual_entry_price 함수는 확인, 컬럼명 미확인 |
| actual_action(실제 행동) / plan_violation_reason / review_memo(기존 메모 겸용?) | **B 확인 필요** | UI 존재 확인, 저장 컬럼명 미확인 |
| quantity / actual_fee / review_done | **C 신규 후보** | 코드·UI에서 미발견 |
| events 테이블 / market_date / data_as_of | **C 신규 후보(2차)** | 1차는 요약문·saved_at으로 대체 |

**Claude Code 읽기 전용 확인 체크리스트** (실행은 상하님 PC에서):
1. `PRAGMA table_info(report_items);` → 위 B·C 필드 존재 여부 대조
2. `PRAGMA table_info(reports);` / `PRAGMA table_info(report_item_outcomes);`
3. app.py에서 실매매 입력 필드의 저장 함수와 컬럼명 추적 (`grep -n "actual_" database.py`)
4. 비용 상수 위치·값 확인 (`grep -n "0.0025\|비용\|fee\|cost" app.py database.py performance.py`)
5. 결과를 설계서 G표에 기입 후에만 작업 9(DB) 착수

## H. 계산식·예외 조건 표 (확정 정의)

| 항목 | 정의 | 예외 |
|---|---|---|
| actual_fee | **금액**(KR=원, US=달러). 매수+매도 수수료+세금 **합계**. 자동값 = 기존 자비스 비용 상수 × (매수대금+매도대금), 수정 가능 | 상수 실측 전 확정값 표기 금지. KR/US 상수 별도 (US 실측률 = 사용자 결정 대기) |
| 실현손익 | (매도가 − 매수가) × 수량 − actual_fee | 수량 NULL → 미계산·경고 |
| 실현수익률 | 실현손익 ÷ (매수가 × 수량) | 동상 |
| R수익률 | (매도가 − **실제 평균 매수가**) ÷ (**실제 평균 매수가** − 계획 손절가). 기존 _compute_result_r 시그니처 유지, entry 인자에 실매수가 전달(실매수가 없으면 계획 진입가로 폴백·화면에 '계획가 기준' 표기) | 손절가 없거나 매수가≤손절가 → 미계산(기존 동작 유지). **수수료 R 미포함(1차)** — 실현손익에만 반영 |
| 평가손익(미청산) | (최근 종가 − 실매수가) × 수량, 수수료 미포함, "참고" 라벨 | 저장 안 함, 화면 계산만 |
| 보유일 | 매도일 − 매수일 (달력일. 거래일 계산은 2차) | 미청산 = 오늘 − 매수일 |
| quantity | **INTEGER** (소수점 주식 1차 미지원) | US 소수점 매수 시 반올림 금지 → 입력 차단·메모 유도 |
| US 환산 | 1차는 **USD 그대로** 기록·표시, KRW 환산 안 함. 시장 통합 통계는 R(무차원) 기준으로만 합산 | 환산 도입 = 사용자 결정 대기 |

## J. KPI·통계 정의표 (④화면)

| 지표 | 분자 | 분모 | 포함 | 제외 | 기간 | 미청산 | NULL |
|---|---|---|---|---|---|---|---|
| 완료 거래 수 | 청산 완료 건 | - | actual_exit 有 | 판단만·보류·제외·진행중 | 필터 기간(매도일) | 제외 | - |
| 승률 | 실현 R>0 건 | 완료 거래 | R 계산 가능 건 | R NULL 건 | 동상 | 제외 | 분모에서 제외 |
| 평균 R / 누적 R | ΣR÷건수 / ΣR | - | 동상 | 동상 | 동상 | 제외 | 제외 |
| 최대 손실 | min(R) | - | 동상 | - | 동상 | 제외 | - |
| 계획 준수율 | plan_followed='예' | '예'+'아니오'+'부분' | 입력 건 | 미입력 | 동상 | 포함 가능 | 미입력 제외 |
| 1순위 적중률 | 1순위 후보 중 5일 가정수익률>0 | 1순위 후보 저장 건 | outcomes 有 | outcomes 無 | 저장일 기준 | 해당 없음(가정) | 제외 |
| 보류했지만 상승 | 보류 판정 & 5일 가정수익률 > **임계값(사용자 결정)** | - | outcomes 有 | - | 동상 | - | 제외 |
| 매수했지만 실패 | 실제 매수 & 실현 R<0 | - | 완료 거래 | 진행중 | 매도일 | 제외 | 제외 |
| 판단 성과 | 판정 방향과 5일 부호 일치 건 | 판정 저장 건 | **기존 performance.py 1·3·5·10·20일 재사용** — 기간 기본 5일 | - | 저장일 | 해당 없음 | 제외 |

신규 기준이 필요한 것: "보류했지만 상승" 임계값 1개뿐. 나머지는 기존 outcomes 재사용.

## I. report_items 확장 vs 별도 trades 테이블 — 최종 결정

| 기준 | report_items 확장 | trades 테이블 |
|---|---|---|
| 구현 난이도 | 낮음 (ALTER ADD COLUMN, NULL 허용) | 중간 (JOIN·저장 흐름 신설) |
| 기존 데이터 영향 | 없음 | 없음 |
| 1종목 1거래 가정 | 정확히 일치 | 과설계 |
| 분할·부분청산 확장 | 불가 → 이전 필요 | 자연 지원 |
| 조회·통계 | 기존 함수 그대로 | 함수 다수 수정 |
| 마이그레이션 위험 | 최소 | 중간 |

**결정: 1차 = report_items 확장.** 이전 기준(다음 중 하나 발생 시 trades 신설): ①분할매수/부분청산 실사용 요구(v1.3) ②같은 report_item에 2번째 거래 필요 ③수수료·체결을 건별 기록 필요. trades 신설 시 최소 필드: id, report_item_id(FK), side, price, qty, fee, executed_at — 거래 없는 종목은 행 없음(LEFT JOIN).

## J-2. 다음 Claude Code 작업 1개 (감리 기준 — 커밋 d01e5ce 다음)

- **작업명**: `KR 화면 저장 흐름 표시 정리 (display only)`
- **목적**: 종목 상세 영역의 저장 관련 버튼·안내를 "▼ 저장 전 확인으로 이동" 개념으로 라벨·캡션만 정리하고, 기존 '국내장 기록 바로 저장' 블록 주변에 구역 안내 캡션(유지/중복 예정) 추가. **버튼의 실제 동작·저장 함수 호출은 변경 금지** — 문구와 markdown 캡션만.
- **위치**: 한국장 탭 종목별 상세 카드 하단 + '③ 국내장 기록 바로 저장' 섹션 상단.
- **재사용**: 기존 렌더 함수 내 st.caption/st.markdown 추가만.
- **수정 대상**: 해당 캡션 출력 블록 (helper 신설 없이 인라인 가능).
- **절대 금지**: 버튼 콜백·저장 함수·위젯 키·DB·계산 변경, 위젯 추가·이동.
- **검증**: py_compile / git diff가 문자열·캡션 라인만 / 저장 1건 정상 동작(상하님 확인).
- **권장 커밋**: `clarify save flow labels (display only)`
- 실제 저장 버튼 단일화(위젯 이동)는 이 작업이 아니라 **그다음 작업**(저장 흐름 변경 등급, 백업 필수)으로 분리.

## K. 사용자 결정 대기 항목 (5개)

1. "보류했지만 상승" 임계값·기간 (제안: 5일 +3%, 기존 outcomes 기간 재사용)
2. 미국장 수수료·세금 실측률 (증권사 기준 확인 후 상수 등록)
3. 미국장 market_date·data_as_of 신규 컬럼 도입 여부 (1차는 요약문 문자열로 충분한가)
4. 같은 종목 재거래 발생 시 처리 (새 report 저장으로 새 item 생성 방식 권장 — 확정 필요)
5. 테마 상태의 DB 이력화 시점

## v2 HTML 자체 검수 결과 (요청 14절)

☑ 최종 저장 실행 버튼 시장별 1개 (KR: confirmKR / US: confirmUS) ☑ "저장 전 확인으로 이동" ≠ "최종 저장" 구분 ☑ KR/US 저장 흐름 동일(리스크→확인 표→차단 사유→최종 저장→성공 이동) ☑ 상단 메뉴(①②/③/④/보조2) = 스테퍼(4단계, 저장결과 제외) 일치 ☑ 저장 결과 읽기 전용·수정 모드 삭제·③입력 버튼 ☑ ③ 복기 체크 삭제·④ 복기 대기 목록 신설 ☑ 상태 7종+오류 배지 표시 ☑ 전 수치·일정 예시 표기(0.25% 확정 표기 삭제→'기존 비용 상수 확인 후') ☑ 실제 코드·DB 무변경.

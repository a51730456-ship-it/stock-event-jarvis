# DECISIONS.md — 자비스2 P2 구현 결정 내역

자율 규칙에 따라 사용자 승인 없이 내린 결정을 기록한다.
(2026-07-16 P2 작업 기준)

---

## 1. rank_limit 기본값 — 2로 확정 (2026-07-16 정정)

- **최초 결정(폐기)**: P2에서 "후보 최대 3개" 문구를 근거로 기본값을 3으로 올렸음.
- **사용자 정정**: "후보 최대 3개"는 대장 카드 표시 개수였고, rank_limit(등수 한계)은
  매매 규칙 "매수는 2등주까지, 3등 금지"였다. 기본값 2가 맞다.
- **추가 발견 버그**: 마이그레이션 방향이 (2→3)이어서 사용자가 설정에서 2로
  바꿔도 재시작마다 3으로 되돌아가는 지뢰였음. (3→2)로 뒤집어 수정.
- **현재 상태**: 기본값 2.0, 마이그레이션은 잘못 배포된 3.0만 2.0으로 복원.

---

## 2. 3R 게이지 = 미청산 진입 건수 기준

- **결정**: 3R 게이지는 playbook_journal의 미청산 건수(is_dropped=0, result IS NULL)
  를 3으로 나눈 값으로 표시. 금액 기반 R 합산은 사용하지 않음.
- **근거**: 기준 R(계좌 크기 등)이 설정에 없어 금액 합산으로 표현 불가.
  "기존 오픈리스크 테이블이 있으면 합산 시도, 불확실하면 플레이북 기준만"
  지시에 따라 플레이북 기준만 사용.
- **한계**: 개별 매매의 R 크기가 다를 수 있어 건수 기준은 근사치임.

---

## 3. 테마 상태 로그 기록 시점

- **결정**: "페이지 로딩 시" 대신 아래 두 시점에 기록
  1. 플레이북 "신호 확인" 버튼 클릭 시 — 선택 테마 1개
  2. "테마판 새로고침" 버튼 클릭 시 — 전체 20개 테마
- **근거**: 페이지 로딩마다 네트워크 호출은 UX 저하. 버튼 클릭 시에는
  어차피 네트워크 조회가 일어나므로 추가 비용 없음.

---

## 4. 급락일 섹션 — 평일 장마감 후 화면 표시

- **결정**: ^KS11 당일 종가 변화율이 -3% 이하일 때만 섹션 렌더링.
  market_data.get_index_daily() 캐시 사용 (파일 캐시, 당일 1회).
- **근거**: 섹션이 항상 보이면 "오늘 급락 아닌데 왜 보이냐"는 혼란.
  장중/장마감 후 캐시 갱신 이후에야 표시됨.

---

## 5. 대장 ticker suffix (.KS)

- **결정**: journal 저장 시 target_ticker에 ".KS" suffix 붙임.
  `sise_group_detail` 파싱은 6자리 코드만 제공; 코스닥 여부를 개별 조회 없이는
  알 수 없으므로 KOSPI 우선(.KS)으로 저장.
- **한계**: 코스닥 종목의 경우 ".KS"로 저장되어 이후 price_data 조회 시
  자동 .KQ 폴백(market_data.get_daily)이 필요함. 현재 get_daily()는 이를 지원함.

---

## 6. 시장상태 새로고침 — 페이지 로딩마다 자동

- **결정**: market_state()는 페이지 로딩 시 자동 호출 (session_state 캐시).
  market_data.get_index_daily()가 파일 캐시(당일 1회)를 사용하므로
  실제 네트워크 호출은 하루 1회에 불과.
- **근거**: 시장상태 스트립은 "항상 위에 보여야 하는 중요 정보"이므로
  버튼 없이 자동 표시.

---

## 7. 경보 확인창 구현 방식

- **결정**: Streamlit native dialog 대신 체크박스 + 조건부 텍스트입력으로 구현.
  경보 있을 때: `st.warning` → `st.checkbox("경보 확인 후 진행")` →
  체크 시 `st.text_input("경보 무시 사유 (필수)")` 노출.
- **근거**: Streamlit에 모달 다이얼로그가 없음. 이 방식이 가장 자연스러운
  단계별 확인 UI.

---

## 8. theme_detail 페이지별 상세 파싱 URL

- **결정**: `sise_group_detail.naver?type=theme&no={id}` 사용 (P0 검증 완료).
  기존 theme_data.py의 `theme.naver?page={n}` 목록 페이지와 분리된 별도 엔드포인트.
- **한계**: 거래대금 단위가 백만원(네이버 표기)이며, 정확한 단위 확인을
  HTML thead에서 확인함 (검증 완료).

---

## 9. 설정 화면 경고 문구

- **결정**: "시작값은 임의 제안 — 30건 증거 전 감으로 수정 금지" 문구 삽입.
- **근거**: JARVIS_CONTEXT.md 핵심 원칙 1 (기대값 우선, 통계 근거 없는 수정 금지).
---

## 10. Turso 원격 DB — upsert 구문 전면 회피 (2026-07-16)

- **관찰**: 배포(Turso/libsql)에서 `INSERT OR IGNORE`, `ON CONFLICT ... DO NOTHING`이
  간헐적으로 ValueError를 냈다 (같은 코드가 한때 성공하기도 함 — 재현 불안정).
- **결정**: playbook 계열 코드는 database.py가 운영에서 검증한 원시 구문만 쓴다:
  단문 CREATE TABLE IF NOT EXISTS / SELECT 후 분기 INSERT·UPDATE.
  executescript도 단문 execute 3회로 분리.
- **방어**: `_get_config()`는 DB 장애 시에도 기본값 dict를 반환해
  페이지 렌더링을 절대 막지 않는다. 테이블 초기화 실패는 경고 로그 후 재시도.

## 11. 배포 반영 검증 방법 (2026-07-16)

- Streamlit Cloud가 push 후에도 옛 빌드를 계속 서빙하는 사례 확인.
- **판별법**: 에러 트레이스백의 줄번호, DOM에 주입된 <style> 내용을
  로컬 커밋과 대조하면 배포된 커밋을 특정할 수 있다.
- 자동 재배포가 안 붙으면 share.streamlit.io → 앱 → Reboot (사용자 액션).

## 12. 옛 모듈이 프로세스에 남는 문제 — MODULE_REVISION으로 감지 (2026-07-24)

- **증상**: 자비스4 눌림목 깔때기가 "전체 0개 · 200억 이상 0개 · 수급 확인 0개"인데
  최종 4개가 나왔다. 한 계산에서 나올 수 없는 조합이다.
- **원인**: Streamlit이 페이지 파일만 다시 읽고 import된 모듈은 프로세스에 유지해,
  화면은 새 코드인데 계산은 f6438cf 이전 jarvis4_data가 돌고 있었다. 그 버전은
  universe_count·liquid_count·flow_checked_count를 반환하지 않았다(그래서 0).
- **왜 기존 방어가 못 잡았나**: 자가복구가 함수 **이름**만 확인해서, 이름은 그대로인데
  내용만 옛것인 모듈을 통과시켰다.
- **결정**: jarvis3_data·jarvis4_data에 `MODULE_REVISION` 상수를 두고, 페이지가
  그 숫자가 기대값보다 낮으면 `importlib.reload`한다.
- **지켜야 할 것**: **계산 결과나 반환 키를 바꾸면 반드시 MODULE_REVISION을 올리고
  페이지의 `_REQUIRED_J3_REVISION`/`_REQUIRED_J4_REVISION`도 같이 올린다.**
  빠뜨리면 온라인에서 옛 계산이 조용히 계속 돈다.

## 13. 자비스5 미니차트 — 시각 기준·짧은 간격 제외 (2026-07-24)

- **증상**: "최근 약 30분" 미니차트가 거의 모든 테마에서 급하게 올라가는 빨간 선이었다.
  사용자가 "코드를 바꾼 적 없는데 왜 이러냐"고 지적.
- **원인 1 (가짜 저점)**: 수집 간격이 32초인 시점이 있었다. 네이버 누적 거래대금은
  몇십 초 늦게 갱신되는데 그 값을 0.53분으로 나눠 분당 환산하면 실제의 1/150이 된다
  (같은 테마 166만 vs 2억5천만). 이 가짜 저점이 맨 앞이라 모든 선이 상승으로 보였다.
  LPG 테마 '직전 대비'가 +15,193%로 나온 것도 같은 원인이며, 걷어내면 -54.8%다.
- **원인 2 (가로축)**: 점을 '몇 번째 수집'으로 균등 배치해, 78분 간격과 271분 간격이
  똑같은 폭으로 그려졌다. 기울기가 거짓말을 했다.
- **원인 3 (기준선)**: 가로 점선을 늘 맨 아래(구간 최저값)에 그려 '0선'처럼 오해를 샀다.
- **결정**: ① 간격 60초 미만 시점은 차트·'직전 대비'에서 제외(`SPARK_MIN_INTERVAL_SECONDS`)
  ② 가로축을 실제 시각 기준으로 ③ 기준선을 구간 시작 수준에 ④ '약 30분' 같은 고정
  문구를 버리고 실제 시간대·점 개수를 표시.
- **주의**: 수집 자체(`activity_intensity`)는 그대로 둔다. 화면 표시만 고쳤다.

## 14. 자비스5 자료를 GitHub Actions로 모은다 (2026-07-24, 사용자 승인)

- **문제**: `run_jarvis5_collector.bat`을 켜 둔 동안만 자료가 쌓였다. 컴퓨터를 껐다 켜면
  자동으로 살아나지 않아, 어제는 82회 수집됐는데 오늘은 수동 5회만 남았다.
- **사용자 요구**: 노트북을 안 켜도 자료가 모이고, 금요일에 끄고 월요일에 켜도
  그 사이 자료가 있어야 한다. 돈은 들지 않아야 한다.
- **결정**: 저장소를 **공개**로 바꾸고(사용자가 직접 전환) GitHub Actions가 장중에
  대신 수집해 저장소에 파일로 올린다. 공개 저장소는 Actions 사용이 무료다.
- **왜 Turso가 아닌가**: 자비스1이 쓰는 Turso에 붙이는 방법도 있었지만, 무료 한도와
  연결 설정이 따라붙는다. 자비스5는 아직 실험 화면이라 저장소 파일이 가볍다.
- **자료 크기 때문에 담는 것을 줄였다**: 종목행 전체는 하루 83만 줄이라 저장소가
  감당하지 못한다. 그래서 **수집 목록 + 테마행 + 마지막 수집분 종목행**만 담는다.
  마지막 수집분 종목행은 다음 수집이 구간값을 이어 계산하는 데 필요하다.
- **형식**: gzip CSV, 유효숫자 8자리. 같은 자료가 JSON 1.87MB인데 이 방식은 0.81MB다.
  실측 대조에서 되돌린 값이 원본과 어긋나지 않았다.
- **수집 id는 파일에 넣지 않는다** — 컴퓨터마다 다르므로 `captured_at`으로 묶는다.
  덕분에 여러 번 합쳐도 중복되지 않는다.
- **작업을 오전·오후로 나눈 이유**: GitHub 작업 하나가 최대 6시간이라 09:00~15:35을
  한 번에 못 덮는다.
- **한계(알고 받아들인 것)**: 예약 시작이 5~20분 늦을 수 있다. 종목 상세는 노트북을
  켠 날 것만 쌓인다.

## 15. 폰 화면 규칙은 미디어쿼리 안에만 둔다 (2026-07-24)

- **배경**: 갤럭시 S21 울트라(412px)에서 자비스3·4를 재 보니 상단 지표 줄이 6줄 838px,
  눌림목 표는 한 종목이 12줄 480px이었다(20종목이면 9,600px). 갤럭시탭 S8+(1138px)는
  같은 화면이 멀쩡했다.
- **결정**: `mobile_ui.py`에 폰 전용 규칙을 모으고 `@media (max-width: 600px)` 안에만 넣는다.
  600px은 탭 S8+(1138px)를 제외하고 폰(412px)만 잡는 값이다.
- **파이썬 렌더링 코드는 건드리지 않는다** — 표의 각 줄은 그 안에 있는 버튼 키를
  `:has(div[class*='st-key-j4pbf_'])`로 집어낸다. 값·점수·판정은 그대로다.
  `:has()`를 모르는 낡은 브라우저에서는 규칙이 무시될 뿐 깨지지 않는다.
- **표는 칸을 숨겨 카드처럼 만든다**: 12칸 중 5칸만 남기고 `::before`로 이름표를 붙였다.
  실측 480px → 94px. 숨긴 값도 계산에는 그대로 쓰이고 종목 상세에 다 들어 있다.
- **게이지는 `order`로 숫자 뒤로 보낸다** — 게이지 셋이 430px를 먹어 KOSPI·수급이
  첫 화면 밖으로 밀렸다. 순서만 바꾼다.
- **표 칸을 더하거나 빼면** `test_mobile_ui.RealPageMappingTests`가 먼저 깨진다.
  폰에서 엉뚱한 칸이 숨겨지는 것을 막기 위한 것이니 칸 번호를 같이 고친다.

# stock_event_jarvis 고정 규칙

## 0. 작업 순서 — 무엇을 하든 이것부터 (2026-08-05 사용자 지시) ⭐

**바로 코드를 수정하지 마라.** 먼저 충분히 분석하고, 가능한 방법을 비교한 뒤,
**가장 안전한 방법**을 골라 구현한다. 성급하게 고치지 말고 **기존 구조를 최대한
유지하는 방향**을 우선한다.

일을 시작하기 전에 반드시 아래 순서대로 한다.

1. 현재 프로젝트 구조를 먼저 분석한다.
2. 기존 코드를 충분히 읽고 수정 계획을 세운다.
3. 수정하기 전에 **어떤 파일을 왜 수정하는지 먼저 설명한다.**
4. 필요한 파일만 **최소한으로** 수정한다.
5. **기존 기능을 절대 깨뜨리지 않는다.**
6. 수정 후 관련 기능이 정상 동작하는지 **스스로 검증한다.**
7. 마지막에 **변경 파일 · 테스트 결과 · 위험 요소**를 보고한다.
8. 작업이 끝나면 **`CURRENT_STATUS.md`를 최신 상태로 갱신한다.**

이 순서는 아래 8번(작업 전 체크)보다 앞선다. 8번은 '이 파일을 읽어라'이고,
0번은 '읽고 나서 어떤 차례로 일하라'다.

### 0-0. 새로 넣는 것이 무엇을 밀어내는지 먼저 잰다 (2026-08-26 상하님 지시) ⭐

**고치려고 넣은 것이 멀쩡하던 것을 망가뜨리지 않게 한다.**
2026-08-26 하루에 같은 실수를 세 번 냈다. 셋 다 상하님이 다시 말씀하셔야 했고,
그날 시간을 가장 많이 잡아먹은 것이 이것이다.

- 미리 계산을 화면 그리기 **맨 앞**에 뒀다 → 첫 화면과 뉴스가 밀렸다.
  (파이썬은 한 번에 한 가지만 계산한다.)
- 뉴스 받기가 **실패하면 옛 뉴스를 지우게** 썼다 → 잘 나오던 카드가 빈칸이 됐다.
- 접이 표의 **모든 줄**을 미뤄 그리게 했다 → 줄이 하나씩 나타나 보였다.

그래서 무엇을 새로 넣기 전에 **반드시 이 셋을 먼저 확인한다.**

1. **뒤에서 도는 일**(스레드·미리 받기·미리 계산)을 넣으려면, 그 시간 동안
   **화면이 무엇을 기다리는지** 먼저 본다. 기다리는 것이 있으면 그 뒤로 미룬다.
2. **실패 처리**를 쓸 때 "실패했으니 지운다"를 기본으로 삼지 않는다.
   있던 것을 그대로 두고 다시 받게 하는 쪽이 화면에서 옳다.
3. **그리는 방식**을 바꾸면 바뀐 뒤 화면이 **어떻게 보이는지**까지 확인한다.
   빨라져도 보기 나쁘면 고친 것이 아니다.

그리고 고친 뒤에는 **전과 후를 숫자로 재서 보고한다.** "빨라졌습니다"가 아니라
"7.9px → 1.8px", "673개 → 358개"처럼 적는다. 못 쟀으면 못 쟀다고 적는다.

## 0-1. 배점·그물·매매 규칙을 손댈 때 (2026-08-12 상하님 지시) ⭐⭐

**이 절은 0번보다도 앞선다.** 2026-08-06~09에 이 규칙이 없어서 상하님이 정하신 것을
세 번 뒤집었고 하루를 통째로 되돌리는 데 썼다. 자세한 내력은 `docs/US_THEME_SPEC.md` 4부.

### 가. 순서 — 과거 차트 → 기준 → 설명서 → 코드
1. `research/`의 스크립트를 **직접 돌려** 지금 숫자를 얻는다.
   **코드 주석·CURRENT_STATUS에 적힌 옛 측정값을 근거로 쓰지 않는다.** 그때는 맞았어도
   명부·그물이 바뀌면 지금은 틀리다.
2. 그 숫자로 기준을 정하고 **상하님께 여쭙는다.**
3. `docs/US_THEME_SPEC.md`·`docs/KR_THEME_SPEC.md`를 먼저 고친다.
4. 그다음 코드를 고친다. **문서보다 코드가 먼저 바뀌면 안 된다.**

### 나. 상하님이 정하신 것은 실측과 달라도 바꾸지 않는다
보고만 하고, 정해 둔 재측정 날짜에 다시 잰다.
**"묻지 말고 해라" · "다 해라" · "그만 물어라"는 마무리를 재촉하는 말이지,
정해진 기준을 바꿔도 된다는 뜻이 아니다.** 그물·배점·명부·매매 규칙을 바꾸는 것은
언제나 **먼저** 여쭙는다. 사후 통보는 여쭌 것이 아니다.

### 다. 한 시장을 고치면서 다른 시장을 같이 고치지 않는다
"한쪽만 검증하면 불공평하다"는 이유로 다른 쪽을 건드리지 않는다.
2026-08-07에 한국을 고치다 미국까지 손대서 상하님 판단을 뒤집었다.

### 라. 그물이나 **명부**가 바뀌면 그 파트를 다시 잰다
명부는 그물과 같은 급이다. 2026-08-09에 종목 하나(CRWD→ORCL)를 바꿨더니
'테마 동반 4개↑'가 합격에서 탈락으로 뒤집혔다.

### 마. 배점 만드는 법
- 합격선 — 창 2·3·4년을 한 달씩 밀고, **그 파트의 그물 안에서** 견주고,
  승률·수익률 **둘 다** 65% 이상. `research/us_verify.py`에 못박혀 있다.
- 점수는 **합격한 항목에만** 준다. 미달·거꾸로·안 잰 것은 0점.
- 계단은 **40 · 30 · 20 · 10 넷뿐이다.** 다섯 번째부터는 안 준다.
- **뺀 점수를 남은 항목에 비례로 나누지 않는다.** 합이 100이 안 되면
  "○○점 만점"이라 적는다. 만점이 곧 그 파트의 근거의 양이다.
- 해당 비율이 **85%를 넘거나 10% 미만**이면 합격이어도 안 쓴다. 못 가른다.
- **재 보고 버린 항목은 「설명」에 남긴다** (2026-08-15 상하님 지시 · 2026-08-19
  상하님이 뜻을 바로잡아 주심). 원래 말씀은 **"각 배점에도 0점짜리도 표시하고
  점수 미달인 이유 넣고"** — 즉 **왜 안 쓰는지를 설명에 적으라**는 뜻이다.
  빼 버리면 앱이 무엇을 재고 무엇을 버렸는지 상하님이 못 보시고, 기준이 두어
  개뿐인 것처럼 보인다(2026-08-15 상하님 물음 — "종목선정근거에 왜 배점이
  하나만 있냐?").
  **어디에 적나 — 배점표가 아니라 배점표 밑 「설명」 창이다.**
  2026-08-19에 상하님이 지적하셨다 — "의미 없는 답인데 상식적으로 생각해도
  넣는 게 문제 아니냐. 0점이라도 설명을 넣으라는 의미는 내가 전달했잖나."
  0점짜리 줄이 배점표에 섞이면 표가 읽히지 않는다. 배점표에는 **점수를 주는
  항목만** 둔다.
  다만 **점수를 줄 가능성이 남아 있는 항목**(다음 재측정에서 되살아날 수 있는
  것)은 배점표에 `0점`으로 남겨도 된다. 이미 답이 나온 것(방향이 반대이거나,
  그물에서 이미 쓴 값이라 영영 겹치는 것)은 배점표에서 뺀다.
  **점수는 어느 쪽이든 그대로 0이다** — 보인다고 점수를 주지 않는다.
- 파는 시점을 정하지 않는 파트는 **여러 보유기간에서 모두 합격한 항목**만 쓴다.
  한 기간에서만 통하는 값은 보유가 바뀌면 뒤집힌다.

### 바. 앱이 하지 않는 일 — 상하님이 판단하실 자리다
1. **파는 시점을 정하지 않는다.** 3개월·6개월·1년 과거 성적을 나란히 보여줄 뿐이다.
2. **얼마를 살지 정하지 않는다.** 나눠 사기는 앱에 넣지 않는다.
3. **빈 자리를 감추거나 딴 것으로 채우지 않는다.** 급락장에 상승장 자리가 없으면
   "오늘은 이 자리가 없습니다"라고 적는다. 자리를 채우려고 기준을 낮추지 않는다.

## 1. 역할 구분
- ChatGPT: 시장 브리핑 작성
- Claude Code: 코드 수정과 테스트
- 자비스 앱: 브리핑 저장과 성과검증
- 자비스 앱은 자동매매 앱이 아니다.

## 2. 절대 금지
- db/jarvis.sqlite3 삭제 금지
- DB 초기화 금지
- 기존 report 삭제 금지
- 기존 report 1~9 삭제 금지
- price_data.py 변경 금지, 사용자가 명시 요청할 때만 변경
- performance.py 계산 로직 변경 금지, 사용자가 명시 요청할 때만 변경
- 증권사 주문 API 추가 금지
- 자동매매 기능 추가 금지
- (해제됨 2026-07-17, 사용자 지시) 실시간 자동조회 허용 — 자비스2 실시간 시세 자동 갱신 적용

## 3. 현재 우선순위
1. 단타/스윙 분리 구조 유지
2. 7필드 간편 입력 유지
3. 보관함/성과검증에서 매매유형 표시 유지
4. 화면 용어·디자인 정리
5. 장중 스냅샷 설계
6. 실시간 조회는 나중

## 4. 테스트 기록 처리
- 테스트 report는 day_conclusion에 반드시 [테스트]를 넣는다.
- 테스트 후에는 해당 report만 삭제한다.
- 실사용 report와 테스트 report를 섞지 않는다.

## 5. 단타/스윙 기준
- 단타: 장중 생존력, 고점 대비 밀림, 시가 대비 위치, 해외선물, 프로그램 수급
- 스윙: 종가, 일봉 추세, 거래대금 지속성, 섹터 지속성, 3일~20일 검증
- 같은 종목도 단타와 스윙 판정을 따로 저장한다.

## 6. 간편 입력 형식
기존 6필드:
종목명 / 티커 / market / 판정 / 신호 분류 / 이벤트명

신규 7필드:
종목명 / 티커 / market / 매매유형 / 판정 / 신호 분류 / 이벤트명

## 7. 판정 매핑
- 보류 → 보류(선반영)
- 관심 후보 → 추천 후보
- 관심종목 → 추천 후보
- 관심 종목 → 추천 후보

## 8. 작업 전 체크
Claude Code는 코드 수정 전 반드시 이 파일을 먼저 확인한다.
작업 후에는 기존 report 1~9가 유지되는지 확인한다.

## 9. app.py 백업 규칙
1. app.py 수정 전에는 항상 app.py 백업을 만든다.
2. 백업 위치는 backups 폴더, 파일명 예시는 app_YYYYMMDD_HHMM.py.
3. 백업 실패하면 작업을 중단한다.
4. app.py 전체 덮어쓰기 금지.
5. 필요한 함수나 블록만 부분 수정한다.
6. app.py 전체 재작성, 전체 덮어쓰기, 대량 치환은 먼저 확인받는다.
7. DB 파일 db/jarvis.sqlite3은 절대 삭제하거나 초기화하지 않는다.

## 10. 자비스5 클라우드 수집 (2026-07-24 도입)
- 자비스5 자료는 GitHub Actions가 장중에 대신 모아 `data/jarvis5/`에 올린다.
  노트북을 꺼 둔 날 자료가 비지 않게 하려는 것이다.
- 저장소는 이 때문에 **공개**다. 비밀번호·토큰·API 키를 코드에 직접 쓰면 안 된다.
  `db/*.sqlite3`와 `.streamlit/secrets.toml`은 계속 .gitignore에 있어야 한다.
- `data/jarvis5/`는 지우지 않는다. 지우면 그동안 쌓인 관측 자료가 사라진다.
- 자비스5 스키마나 수집 계산을 바꾸면 `jarvis5_sync.py`의 필드 목록도 같이 고친다.
- 자세한 이유와 구조: `docs/JARVIS5_CLOUD_COLLECTION.md`

## 10-1. 날짜별 목록 저장 (2026-08-09 도입)
- 네 갈래(눌림목 찾기·상승장·급락 후 반등장·순위 7)의 그날 목록은
  `data/picklist/{날짜}.{US|KR}.csv`에 **찍은 그대로** 쌓인다. **지우지 않는다** —
  지우면 "그때 목록이 맞았나"를 잴 근거가 사라진다.
- 저장하는 쪽은 **값을 다시 계산하지 않는다.** 화면이 부르는 함수를 같은 인자로
  부르고 결과를 그대로 옮긴다(`picklist_collector.py`). 여기에 점수 계산을 따로
  쓰면 저장된 목록과 화면 목록이 조용히 갈라진다.
- 못 잰 값은 **빈칸으로 둔다. 0으로 채우지 않는다.**
- 자동 저장은 GitHub Actions(`.github/workflows/picklist_collect.yml`)가 한다 —
  상하님이 로그인하지 않아도 쌓여야 한다는 것이 이 기능의 요구다.
- 저장 칸을 바꾸면 `picklist_store.SCHEMA_VERSION`과 `MODULE_REVISION`을 같이 올린다.

## 11. 모듈 리비전 규칙 (2026-07-24 도입)
- jarvis3_data·jarvis4_data의 **계산 결과나 반환 키를 바꾸면** 그 파일의
  `MODULE_REVISION`을 올리고, 페이지의 `_REQUIRED_J3_REVISION`/`_REQUIRED_J4_REVISION`도
  같은 값으로 올린다.
- 빠뜨리면 온라인에서 옛 모듈이 프로세스에 남아 옛 계산이 조용히 계속 돈다
  (2026-07-24 실제 발생: 눌림목 깔때기 숫자가 전부 0으로 나왔다).

## 12. 폰 화면 규칙 (2026-07-24 도입)
- 폰 전용 표시 규칙은 `mobile_ui.py`에만 두고 `@media (max-width: 600px)` 안에만 넣는다.
  밖으로 새면 갤럭시탭 S8+(1138px)·PC 화면까지 바뀐다.
- 표의 칸을 더하거나 빼면 `pages/*.py`의 `mobile_ui.table_css(...)` 칸 번호도 같이 고친다.
  안 고치면 폰에서 엉뚱한 칸이 숨겨진다(`test_mobile_ui`가 먼저 깨진다).
- 폰 규칙은 보여주는 방식만 바꾼다. 값·점수·판정은 건드리지 않는다.
- 로그인 화면의 '로그인 후 이동' 목록과 '어디로 갈까요' 화면의 목록은
  폰·태블릿(≤1200px)에서 **미국테마·한국테마 둘만** 보이게 한다(2026-08-01 지시).
  앞 3개(시장판단·자비스1·자비스2)와 뒤 2개(선행감지·종가관찰)를 CSS로 숨긴다.
  옵션 자체는 지우지 않는다 — 노트북/PC에서는 7개가 다 보여야 하고, 나중에
  되살릴 수 있어야 한다. 숨기는 항목이 기본 선택이 되면 안 된다
  (app.py `_DEST_DEFAULT_INDEX = 4` 한국테마).
  목록은 app.py `_DEST_OPTIONS` 한 곳에 있다. 순서를 바꾸면 위 nth-child 번호도
  같이 고친다.

## 13. 로그인 유지 규칙 (2026-07-25 도입)
- 로그인 확인은 `auth.py`의 `sync_auth()`를 게이트 맨 앞에서 부르는 방식이다.
  게이트는 6군데(app.py + pages 5개)에 있으니 새 페이지를 만들면 거기도 넣는다.
- 쿠키에는 **비밀번호를 넣지 않는다.** `sha256(소금+APP_PASSWORD)` 해시만 담는다.
  저장소가 공개라 비밀번호가 코드·쿠키 어디에도 남으면 안 된다.
- 쿠키가 없거나 실패하면 조용히 넘어가 예전처럼 세션 기반으로 동작해야 한다.
  이 '조용한 실패'를 없애면 온라인에서 로그인이 깨져 못 들어가는 사고가 난다.
- `clear_auth()` 뒤에 곧바로 `st.rerun()`을 하지 않는다. 쿠키 삭제는 화면에
  그려지는 컴포넌트가 실행하는데 rerun이 그 렌더를 취소해 쿠키가 남는다
  (2026-07-25 실측). 지금 앱에는 로그아웃 기능이 없다.

## 14. 사용자 설명 선호
- 사용자는 너무 짧거나 지나치게 긴 설명보다, 핵심이 잘 정리된 충분한 설명을 선호한다.
- 설정이나 조작 방법은 꼭 필요한 클릭 순서와 화면 문구를 번호로 간결하게 안내한다.
- 먼저 핵심 방법을 설명하고, 문제 해결이나 대체 방법은 실제로 필요할 때만 추가한다.

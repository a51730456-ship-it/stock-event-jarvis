# 자비스 사용법

AI(Claude) 없이 자비스를 켜고 사용하는 방법을 정리한 문서입니다.

## 1. 자비스 실행 방법

- `자비스실행.bat` 더블클릭
- 검은 실행창이 떠 있어야 정상 (이 창이 자비스 서버입니다)
- 검은 창을 닫으면 자비스가 종료됩니다

## 2. 노트북 접속 방법

- 브라우저에서 `http://localhost:8501` 접속
- 비밀번호 입력

## 3. 스마트폰 접속 방법

- 스마트폰은 노트북과 같은 와이파이에 연결
- 홈화면 자비스 아이콘 실행
- 안 되면 실행창에 나오는 Network URL 확인
  - 예: `http://172.30.1.3:8501`
  - IP는 노트북 네트워크 상황에 따라 바뀔 수 있음

## 4. 비밀번호 변경 방법

1. `.streamlit/secrets.toml` 파일 열기
2. `APP_PASSWORD = "..."` 줄에서 따옴표 안 값만 변경
3. 저장
4. 자비스 재시작 (검은 창 닫고 `자비스실행.bat` 다시 실행)

주의:
- 비밀번호는 절대 Git에 올리지 않습니다 (secrets.toml은 커밋 대상이 아님)
- 실제 비밀번호 값은 이 문서를 포함해 어떤 문서에도 적지 마세요

## 5. 자동 백업 설명

- 앱 시작 시 하루 1회 DB를 자동 백업합니다
- 위치: `backups/jarvis_YYYYMMDD.sqlite3`
- 같은 날짜 백업 파일이 이미 있으면 덮어쓰지 않습니다

## 6. 정상 상태 기준

아래와 같으면 정상입니다.

- report 14건
- report_items 62건
- rejected_items 0건
- report 1~9 보존
- git status는 `.claude/`만 남아도 정상

## 7. 현재 주요 기능 요약

- 필터 무시 기록
- 필터 준수 vs 필터 무시 R 비교
- 최악 5매매 R 점검
- 총 오픈 리스크 3R 점검
- 플레이북 태그
- 탈락 종목 기록
- 자동 백업
- 비밀번호 잠금
- 스마트폰 접속
- 더블클릭 실행

## 8. 문제 생겼을 때

- 자비스가 안 열림: `자비스실행.bat` 다시 실행
- 스마트폰 안 됨: 같은 와이파이인지 확인
- 비밀번호 안 됨: `secrets.toml` 값 확인 후 서버 재시작
- 검은 창이 닫힘: 자비스가 꺼진 것 (다시 실행하면 됨)
- DB는 직접 건드리지 말 것

# JARVIS_CONTEXT.md — 자비스 프로젝트 배경 설명서

## 프로젝트 정체성
자비스는 자동매매 프로그램도 매수 추천기도 아니다. 
사용자(상하님)의 단타/스윙 매매에서 나쁜 행동을 줄이고, 기록을 통해 
기대값 있는 조건을 찾아가는 개인용 판단 보조 도구다.
"GPT 브리핑 검증기"에서 "본인 매매 판단·습관 검증기"로 정체성이 진화했다.

## 핵심 설계 원칙
1. 승률보다 기대값·최대손실이 우선 지표. 승률 단독 강조 금지, 항상 기대값·최대손실과 함께 표시.
2. 필터 무시 UI가 핵심 실험 장치 — "필터 준수 매매 vs 무시 매매" 손익 차이를 숫자로 증명하는 것이 앱의 핵심.
3. 뉴스·공시·시황·테마는 점수·판정·DB 저장에 반영하지 않는다 (참고용일 뿐).
4. 기존 계산·판정·저장·widget key·session state는 보호한다 — 대규모 리팩터링 금지.
5. 결제 수단 없이 동작 가능한 무료 구조로 설계 (무료 API 티어 등).

## 기능 우선순위 (A~J, 확정)
- 1군(2026-07-12 검증/수정 완료): A(필터 무시 로그 — 이미 구현, 검증만 PASS),
  E(총 오픈 리스크 3R 제한 — 계산이 실제 R단위가 아니던 버그 발견·수정, 커밋 7f54351),
  D(플레이북 태그 — 이미 구현, 검증만 PASS)
- 2군(2026-07-12 검증 완료): I(청산 시나리오 기록 — 이미 구현, 정상 저장·차단 규칙 모두 검증 PASS),
  B(탈락 종목 추적 — 이미 구현, UI·DB 저장/조회 검증 PASS)
- 3군: C(조건별 기대값 통계, N≥30 시점에 착수)
- 보류: J(통계 근거 없는 차단 추가 금지)
- 위험한 방향(하면 안 되는 것): 예측시장 점수화, 뉴스 자동 가점, AI 추천 직결, 승률 중심 화면, 
  소액베팅 확대, 기록 마찰 높여 앱 밖 매매 유도, 성적 나쁠 때 기준 소급 변경

## UI 구조 — 확정된 6개 상위 탭
① 한국장 판단 / ② 미국장 판단 / ③ 행동·청산 / ④ 복기·통계 / ⑤ 기록 조회 / ⑥ 보조

## Fable 5가 확정한 사용자 요구 12개 (jarvis_design_spec_v2.md 근거)
1. 중요한 기능과 결과가 화면 위쪽에 있어야 한다
2. 글자는 충분히 크고 문단·구역 간격이 분명해야 한다
3. '오늘 종목 판단 준비하기' 버튼 하나로 0→1→2 실행
4. 개별 재실행 버튼은 오류 대응용 보조 기능
5. 전체 후보 종목은 항상 비교 가능하게 보여야 한다
6. 후보 종목 선택 시 선택값과 상세 내용 전체가 함께 바뀌어야 한다
7. 상세 입력은 선택 종목 1개만 렌더링
8. 전체 테마는 보이되 세부 입력은 선택 테마 1개만 표시
9. 태블릿에서 내용 잘리거나 화면 밖 밀림 금지
10. PC에서 잘 쓰던 기능·후보 종목을 사용성 개선 이유로 삭제 금지
11. 뉴스·공시·시황·테마는 점수·판정·DB 저장에 반영 금지
12. 기존 계산·판정·저장·widget key·session state 보호

## 지금까지 발견 및 수정한 것 (이 세션 기준)
1. ✅ [수정완료] 후보 카드 클릭 시 앱 크래시 버그 
   - 원인: st.selectbox(key="mockup1_selected_ticker")가 위젯 인스턴스화 후, 
     같은 run 안에서 버튼이 같은 키를 재대입해서 StreamlitAPIException 발생
   - 수정: mockup1_pending_ticker 별도 키 우회 방식으로 해결, AppTest 3개 PASS, 커밋 eb488a0
2. ✅ [수정완료] FABLE MOCKUP 01 안내 배너가 HTML 소스에 남아있던 것 제거
3. ✅ [수정완료] "테마 참고판"과 "오늘 강세 테마 1차 참고판" 중복 — 후자는 버튼도 입력도 없는 
   죽은 안내 블록으로 확인되어 제거, 커밋 86083d0
4. ✅ [조사완료, 수정 안 함] 한국장 저장 버튼 2개(kr_quick_save, kr_quick_confirm_save)는 
   중복이 아니라 "미리보기 확인 → 최종 저장" 의도된 2단계 안전장치. 유지 결정.
5. ✅ [검증완료] Fable 5 확정 요구사항 12개 중 6,11,12 포함 전부 AppTest/코드로 검증 완료.
6. ✅ [수정완료] 태블릿(768px)에서 테마 참고판 데이터프레임이 화면 밖으로 밀리던 버그.
   overflow-x: auto 1줄 추가로 해결, 커밋 0817e66.
7. ✅ [수정완료] 요구사항 11 위반 발견 — 한국장 참고점수(compute_snapshot_reference_score)에서
   시장 분위기(시황) 입력이 점수 상한을 88→100으로 올려주고 있었음. external_good 로직 제거,
   커밋 b0c28c4.
8. ✅ [수정완료] 같은 문제가 미국장에도 있었음 — compute_us_swing_breakdown의 총점 계산에
   mood_score(0~15점)가 직접 합산되어 판정(verdict)까지 좌우했음. 총점 계산에서 제외
   (표시는 참고용으로 유지), 커밋 d06c76c.
9. ✅ [정리완료] jarvis_design_spec_v2.md에 "C~K는 제안, 마지막 섹션이 실제 구현"이라는
   안내문 추가 + "저장 결과" 용어가 문서(재확인)와 실제 앱(성과검증)에서 다르게 쓰인다는
   주석 추가. 문서만 수정, 코드/DB 무변경. 커밋 6e67bf6.

## 아직 미검증/미완료 항목
- (2026-07-12 기준) 없음. Fable 5 확정 요구사항 12개 전부 검증 완료, 발견된 문제 전부 수정
  완료, 문서 혼동 이슈도 정리 완료.

## 결정 로그
- 2026-07-13: 테마 시작일 기준 충돌 해소. Fable5의 원 기준([답1] "테마 내 3종목 이상 동시
  상승이 처음 확인된 날=D1", 수동 입력)은 폐기. 최종 확정 기준은 자동 방식 —
  theme_history.py가 테마 참고판 자동조회 클릭마다 쌓는 날짜별 강함/보통/약함 로그에서
  연속 "강함" 스트릭을 역산해 경과일수 자동 표시(4일 이상 시 추격주의 배지). 사유(Fable5):
  수동 3종목 확인은 사람이 매일 세야 해서 실행 안 됨, 자동 로그는 버튼만 누르면 쌓여 실행
  가능성이 높음, 완벽한 기준보다 매일 돌아가는 근사 기준이 낫다(유효성은 추후 태그 데이터로
  검증). 커밋 bf06b45.

## 2026-07-13 작업 요약 (Fable5 3단계 계획 2·3단계)
- 2단계-B 대장주 상태 하이브리드 토글(SNAPSHOT_STOCKS 7종목 안이면 고점대비 -7% 자동
  "꺾임", 밖이면 수동, 사용자가 항상 덮어쓸 수 있음). 커밋 a0f54d5.
- 2단계-C 플레이북 태그에 "테마-대장주"/"테마-2등주"/"테마-3등이하" 3종 추가(DB 스키마
  무변경). 커밋 6e345ab.
- 3단계 행동 데이터 필드: report_items.entry_time/auto_loss_streak_flag/
  earnings_check_done 컬럼 + app_settings 테이블(하루 신규 진입 경고 기준값) 추가. "매수"로
  바뀌는 순간 진입시각 자동기록 + 직전 손절 2건이 오늘/어제 안이면 "손실직후" 자동 태그.
  실적 발표일 확인 체크박스(미국장 스윙 전용), 하루 신규 진입 경고(참고용, 차단 없음).
  "계획 보유일수"는 기존 expected_holding_days 컬럼 재사용(신규 컬럼 안 만듦). 커밋 4ab85a4.
- **중요 버그 발견·수정(오늘 작업과 무관, 이전부터 있던 문제)**: `_render_actual_trade_entry_
  inputs`/`_render_trade_exit_inputs`(③ 실제 행동·거래 종료 탭의 핵심 함수) 약 280줄이
  실수로 `if _saved_view == "지난 기록":` 조건 안에 들여쓰기 되어 있어서, 앱을 새로 열어
  ⑤ 보관함 > 지난 기록을 한 번도 안 본 상태로 ③ 탭에서 종목을 선택하면 NameError로
  화면이 깨지는 상태였다(커밋 bf06b45에서도 재현 확인, 이번 세션 작업으로 생긴 문제 아님).
  조건문 밖으로 4칸 dedent해서 수정, 로직 변경 없음. 커밋 107346f.
- Fable5 3단계 계획 중 남은 항목: X/Y 조사(필터 무시 로그 감정 태그 실제 연결 여부 / 탈락종목
  자동 가격조회 여부). → **X/Y 둘 다 조사 완료.** X: 감정 태그 실제 연결됨(정상). Y: 탈락종목
  자동 가격조회 기능 존재하지 않음(미구현, 필요하면 별도 요청).
- 7/12 스크린샷 지적 UI 7건 전부 처리: 2번(라벨-드롭박스 간격)·3번(후보카드 간격)·7번(모바일
  버튼 위계) 실제 CSS 버그 수정, 4·5·6번 문제없음 확인, 1번 폐기(상하님도 기억 못함). 커밋
  cbbed61, 0c99732.
- **전체 코드베이스 울트라리뷰(2026-07-13, 6개 에이전트 병렬).** app.py/database.py/
  performance.py/price_data.py/theme_data.py/theme_history.py 전부 정밀 검토. 실제
  버그 9건 발견·수정(탭 밖 렌더링 2건 추가 발견 — 오늘요약/수동기록입력, 저장 전체를
  막던 검증 버그, falsy-zero 점수 버그, 죽은 필터 조건, CSS 충돌 잔여분, 실매매 성과
  캐시 충돌, DB 상태모순 가드 확장, theme_history.py 커넥션 leak, theme_data.py 부분실패
  처리). 커밋 fd229e2, d29bb10, 872c951. **performance.py에서 미국장 스윙 "다음 거래일"
  계산이 한국시간/미국동부시간을 안 맞춰 하루 밀리는 구조적 버그 발견 — CLAUDE.md 보호
  파일이라 미수정, 상하님 확인 필요(다음 세션 최우선).** price_data.py 일봉차트 검증
  게이트도 경미한 이슈 있으나 보호 파일이라 미수정.

## 자비스2 — 순환매 플레이북 페이지 (2026-07-16 추가)

### 파일 구조
- `pages/1_자비스2.py` — Streamlit 멀티페이지 자비스2 본체 (신규)
- `market_data.py` — 일봉 조회 + 지표 계산 (P1 신규)
- `theme_detail.py` — 네이버 sise_group_detail 구성종목 파싱 (P1 신규)
- `playbook.py` — 판정 함수 6개 + DB 헬퍼 + 테이블 초기화 (P1 신규)
- `DECISIONS.md` — P2 자율 결정 내역 기록 (신규)

### 신규 DB 테이블 (기존 테이블 무변경)
- `playbook_config`: 8개 설정값 (near_high_pct, value_mult, min_value_eok,
  max_spike_pct, entry_max_age, leader_break_pct, rank_limit, volatile_days_warn)
- `playbook_journal`: 진입/탈락 기록 (setup, entry/stop/qty/r_amount, alert 이력, tags)
- `crash_log`: 급락일 원인 가설 + 보유논리훼손 기록

### 페이지 구성 (위→아래)
1. 시장상태 스트립: market_state() — 하락국면 OR 60일 변동일수 ≥ 12 시 경고
2. 순환매 플레이북: 테마선택→신호확인→대장카드→매수대상→경보→기록
3. 급락일 기록: 코스피 -3% 이하인 날만 표시 (당일 캐시 기반)
4. 테마판 요약: 전체 20개 테마 등락률·연속강세일 표
5. 판단 기록: playbook_journal n/30 진행바 + 최근 카드
6. 설정: playbook_config 8개 값 편집

### 경보 정책
- max_warning (최근 20일 내 +20% 이상 급등) → 경보 배지
- leader_break (최근 고점 -7% 이상 하락) → 경보 배지
- 테마나이 D+4 이상 → 추격 주의 배지
- 경보는 차단 아닌 확인창 — 무시 시 사유 텍스트 필수

### 알려진 한계 / 미완료 항목
- theme_state_log 축적은 "신호 확인" / "테마판 새로고침" 버튼 클릭 시에만 발생
  (페이지 로딩 자동 축적 아님 — UX 트레이드오프)
- target_ticker 저장 시 ".KS" suffix 고정 (코스닥 종목도 동일 — get_daily가 폴백 처리)
- 3R 게이지는 건수 기준 (계좌 크기 미설정으로 금액 기반 불가)
- 테마 상세 페이지(sise_group_detail) 거래대금 단위: 백만원 (네이버 thead 확인 완료)
- crash_log UI는 오늘 코스피 -3% 이하 당일만 표시됨 (과거 급락일 소급 입력 불가)

### v1.1 백로그 (2026-07-17 — 30건 기록 전 착수 금지)
"52주 신고가+테마" 매매기법 정밀화 과정에서 확인된 자동화 갭. 기록만, 지금 구현 금지:
- 청산 신호 자동 감지 (무거래 신고가 갱신 / 장대음봉+거래량 증가 — 일봉 데이터로 가능)
- 테마 전체 거래대금 합계·증감 추이 표시 (네이버 스크래핑 데이터로 계산 가능)
- 기관·외국인 수급 데이터 (신규 스크래핑 필요 — 위험도 높음)
- 단타 진입타임 표시 카드 (가속 RSI 방식 — project_entry_timing_signal 메모리 참고)

## 작업 원칙 (항상 지킬 것)
- 코드 수정 전 sqlite3 backup API로 반드시 백업 (shutil 금지)
- 저장 로직·DB 관련 변경은 조사 후 먼저 보고, 승인 받고 나서 수정
- 화면 표시만 건드리는 저위험 작업은 바로 진행 가능
- 수정 후 AppTest로 실제 클릭 시뮬레이션 검증 필수 (grep으로 존재만 확인하는 건 불충분)
- 테스트 데이터는 저장 후 검증 즉시 삭제해서 DB를 테스트 전 상태로 복원
- 매 작업마다: (1) 이번 작업이 뭔지 (2) 총 진행률 % (3) 남은 작업 목록을 보고할 것

# 스트림릿 설정. 비밀은 여기 넣지 않는다 — 저장소가 공개다(CLAUDE.md 10번).
[server]
# static/ 안의 파일을 app/static/<이름>으로 내보낸다 (2026-08-27 상하님 지시).
# 설명 창의 만화·표를 눌렀을 때 새 창에 원본을 띄우려면 이 길이 있어야 한다.
# 창 안에서는 두 손가락으로 벌려도 창이 닫혀 버려 크게 못 본다.
enableStaticServing = true

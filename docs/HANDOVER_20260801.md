# 인수인계 — 2026-08-01 (다음 대화방에서 이어서)

이 방에서 한 일과 **남은 일**을 적는다. 다음 사람이 이 문서만 읽고 이어갈 수 있어야 한다.
마지막 갱신: 2026-08-01 저녁. 남은 일은 **③ 나스닥 주봉 신호 하나뿐**이다.

---

## 0. 가장 먼저 알아야 할 원칙

이 방에서 세운 규칙이다. **어기면 화면이 거짓말을 한다.**

1. **재 보지 않은 값으로 순위를 매기지 않는다.** 좋아 보여도 실측에서 갈리지 않으면 뺀다.
2. **성적 옆에는 늘 기준선('아무 날이나 샀으면')을 적는다.** 오늘 살아남은 종목만 보고
   재기 때문에, 같은 종목으로 잰 기준선과 견줄 때만 치우침이 상쇄된다.
3. **진 것은 졌다고 적는다.** 낙폭 -30~-40% 갈래는 한국에서 기준선보다 못했고, 화면에
   그대로 적혀 있다.
4. **미국과 한국은 배점이 다르다.** 시장도 자료도 실측 결과도 다르다. 같은 자로 재면 안 된다.
5. 화면 문구·코드값·문서 숫자가 어긋나면 **시험이 먼저 깨지게** 묶어 두었다.

---

## 1. 이 방에서 끝낸 일

### 화면
- 미국·한국 상단 카드 글자 크기/색, SPY·QQQ 당일+일봉 그림
- 두 화면에 **설명서 두 갈래 단추**(상승장 신고가 눌림매수 / 급락 후 반등장 낙폭종목)
- 뒤로가기 → 자비스1 대신 **'어디로 갈까요' 선택화면**(app.py, 주소 표식 `?page=jarvis1`)
- 로그인 목록·사이드바를 폰·태블릿에서 **미국테마·한국테마 둘만**
- 표를 폰에서 옆으로 밀어 보기(`j3_rulebook_table`·`j4_rulebook_table` 포함)
- 종목 누르면 상세·당일차트·일주월봉이 **한 번에** 열림 + 구역마다 **아래에도 닫기 단추**
- 열려 있는 갈래 단추에 ● 표시와 흰 테두리
- 태블릿 가로/세로에 따라 상단 칸 수가 바뀜(`mobile_ui` 방향별 미디어쿼리)

### 설명서 (`method_help.py`)
- 미국: 사용자가 준 '미국장 눌림목 매매 설명서' 원고 그대로 + 순위·배점 + 왜 수급 칸이 없는지
- 한국: 같은 옷으로 새로 씀 + 한국 자체 검증값 + 왜 동반 수급을 뺐는지
- 길이 상한 2,600자(보이는 글자)를 지킨다. `test_method_help.LengthTests`가 지킨다.

### 검증 (문서로 남김)
- `docs/KR_RULE_BACKTEST.md` — 한국 12년치
- `docs/US_RANK_BACKTEST.md` — 미국 10년치, **버린 후보와 그 이유**까지

---

## 2. 남은 일

### ①② 두 갈래 전용 배점 — **끝났다 (2026-08-01 저녁)**

미국·한국 모두 갈래별 배점을 만들고 **화면까지 연결했다.** 낙폭 종목을 눌러도 더는
14~26점 '제외'가 뜨지 않는다.

- 미국: `jarvis3_data.BREAKOUT_SCORE_WEIGHTS`(테마 35 / 60일 상승폭 30 / 눌린 폭 15 /
  유동성 10 / 변동성 10, **거래대금 연속 0점**) · `breakout_score` · `breakout_plan` ·
  `_breakout_rank_key`
- 한국: `jarvis4_data.BREAKOUT_SCORE_WEIGHTS`(**거래대금 30** / 테마 30 / 60일 상승폭 25 /
  **거래대금 연속 15**) · `BREAKOUT_TOGETHER_TIERS`(1·2개) · `_breakout_rank_key`.
  한국 자료로 따로 쟀다(1,779자리 · 187종목 · 2015-06~2026-01, 기준선 57번).
- 화면: 두 페이지의 `_render_pullback_detail`이 갈래에 따라 다른 자를 쓴다. 심사 결과
  칸은 기준가·손절 대신 **사는 때 · 보유 기간 · 파는 때 · 손절 없음**을 적는다.
  `guidance.build()`에 `rule_mode` 갈림을 넣어 시장 점수 문턱이 잘못 붙지 않게 했다.
- 표 칸: 상승장에서는 '거래대금(평소 위 연속)'을 빼고 **'최근 60일 상승폭'**을 넣었다.
  낙폭 표는 그대로다.
- 설명서(`method_help`)의 순위·점수 상자도 두 갈래로 갈라 다시 썼다.

**미국과 한국이 정반대인 것 — 지우지 말 것**: 거래대금 평소 위 연속은 미국 상승장에서
거꾸로(53번)라 0점이지만, 한국 상승장에서는 값을 한다(63번). 코드 주석과
`docs/KR_RULE_BACKTEST.md`·`docs/US_RANK_BACKTEST.md`에 근거를 적어 두었다.

### ③ 나스닥 주봉 반등 신호 — **결론은 났고 화면에 안 넣음** ⬅ 여기부터

사용자 기준은 "MACD와 STC 둘 다 침체". 55년 주봉으로 24개 설정에서 재 봤다.

- **"둘 다 침체"만 보면 24개 중 23개에서 기준선보다 나빴다**(52~67번 vs 70번).
  침체는 하락장 한복판이지 반등 신호가 아니다.
- **"침체 + MACD 막대 3주 연속 커짐"은 24개 중 16개에서 기준선을 이겼고, 24개 전부에서
  침체만 볼 때보다 좋아졌다.** 방향이 일관되므로 설정 탓이 아니다.
- **다이버전스는 6개 설정 중 0개에서 이겼다.** 섞으면 오히려 나빠진다(77→71번). 쓰지 말 것.
- 빈도: 12/26 설정에서 55년에 28번 ≈ **2년에 한 번**. 사용자가 원한 "1~2년에 한 번"과 맞는다.

**막힌 것** — 숫자가 설정에 따라 59~81번으로 벌어진다. **사용자의 STC 설정값(23/50인지
다른 값인지)을 물어봐야** 사용자가 실제로 보는 화면과 같은 숫자를 낼 수 있다.
설정을 받기 전에는 화면에 넣지 말 것.

### ④ 자잘한 것 — 둘 다 정리됨

- **한국 종목검색 칸 이름** — 미국과 같게 맞췄다(2026-08-01).
  "종목이름 또는 종목코드 (아래에 종목이름을 넣어보세요)" · 같은 보라색(#a855f7).

- **PC에서 사이드바 '자비스1'을 누르면 '어디로 갈까요'를 한 번 거친다** — 그대로 두기로
  했다(2026-08-01 사용자 결정). **고치려다 재 보고 안 되는 것을 확인했으니 다시
  시도하지 말 것.**

  빈 시험용 앱으로 실제 눌러 보고 주소를 확인한 결과:

  | 해 본 것 | 주소 표식(`?page=…`) |
  |---|---|
  | 사이드바로 첫 화면 가기 | **사라진다** |
  | `st.switch_page()`로 옮기기 | **사라진다** |
  | 코드로 표식을 남기고 그대로 머물기(`st.rerun()`) | 남는다 |
  | 그 뒤 뒤로가기 | 되살아난다 |

  '표식 있으면 선택화면'으로 뒤집는 것도 만들어서 눌러 봤다 — 로그인 뒤 다른 페이지로
  옮기는 순간 첫 화면의 표식이 지워져 뒤로가기가 다시 자비스1로 간다.

  **결론** — 뒤로가기와 사이드바 클릭은 **둘 다 맨 주소(`/`)로 온다.** 서버에서 가를
  방법이 없다. 사이드바 기본 항목을 감추고 `/?page=jarvis1` 링크를 직접 다는 길만
  남는데, 페이지가 통째로 새로 떠서 오히려 느리고 사이드바 코드 6곳을 고쳐야 한다.
  선택화면은 무거운 자료를 하나도 안 읽어 바로 뜨므로 지금이 낫다.

---

## 3. 손대면 안 되는 것 / 알고 있어야 할 함정

- **`test_bookmaker_data.py::PolymarketEventGroupingTests` 1건은 원래 깨져 있다.**
  이 방에서 건드리지 않은 파일이고, 작업 전부터 실패했다. 다른 실패와 혼동하지 말 것.
- **PowerShell `Set-Content -Encoding utf8`은 BOM을 넣는다.** 이 방에서 파일 9개에 BOM이
  들어갔다가 걷어냈다. 파이썬 파일을 고칠 때는 `Write`/`Edit` 도구나 파이썬으로 쓸 것.
- **PowerShell here-string(`@'...'@`)에 큰따옴표가 들어가면 git 인자가 쪼개진다.**
  커밋 메시지는 파일로 만들어 `git commit -F`로 넣을 것.
- **모듈 리비전(규칙 11)** — `jarvis3_data`/`jarvis4_data`/`method_help`/`mobile_ui`를 고치면
  그 파일의 `MODULE_REVISION`과 페이지의 `_REQUIRED_*_REVISION`을 **같이** 올린다.
  안 올리면 온라인에 옛 코드가 남는다(이 방에서 실제로 "설명서가 안 바뀐다"로 겪었다).
- **표를 새로 만들면** 페이지 `<style>`의 옆으로 밀기 목록 **세 곳**에 다 넣어야 한다.
  빠뜨리면 폰에서 칸이 세로로 쌓이고 값이 겹쳐 찍힌다.
- **화면이 열릴 때마다 두 번 그려진다**(쿠키 로그인 컴포넌트 탓). 첫 판을 `st.stop()`으로
  끊는 방법은 시도했다가 되돌렸다 — 쿠키가 막힌 곳에서 화면에 갇힌다.
  자세한 것: 메모리 `project_double_render_cookie_login`.

---

## 4. 검증에 쓴 자료 (다시 재려면)

임시 폴더에 받아 둔 것이라 다음 방에서는 **다시 받아야 한다.** 받는 코드는 아래 문서에 적혀 있다.

| 자료 | 어디서 | 얼마나 |
|---|---|---|
| 한국 일봉 | 네이버 `fchart.stock.naver.com` count=3000 | 197종목 · 2014-05~ (12년) |
| 한국 수급 | 네이버 `finance.naver.com/item/frgn.naver?page=N` | 80종목 · 150쪽 = 12년 |
| 미국 일봉 | yfinance `period="10y"` | 198종목 · 2016-08~ |
| 미국 공매도 | FINRA `cdn.finra.org/equity/regsho/daily/` | 1,255일(5년), 무료 |

**네이버 자료는 검사했다** — 일봉은 FinanceDataReader와 1,615일 100% 일치, 빠진 날 0일,
수급 부호는 보유주수 변화와 87~94% 일치. 다만 **기관 순매매량은 대조할 상대가 없다**
(KRX·pykrx 둘 다 로그인 요구).

방법과 결과: `docs/KR_RULE_BACKTEST.md`, `docs/US_RANK_BACKTEST.md`

---

## 5. 재 보고 **버린** 것 — 다시 제안하지 말 것

| 후보 | 왜 버렸나 |
|---|---|
| 미국 '담는 날 5일'(오르며 거래량 평균 이상) | 평평, 많을수록 오히려 나쁨 |
| 미국 담기/털기 비율 | **거꾸로** |
| 거래대금 급증(오늘÷20일 평균) | 규칙 없음 |
| 논문식 거래대금 순위(GKM 2001) | 대형주에서는 안 나옴 |
| FINRA 공매도 비중 | 평평·비단조. 문헌도 "대형주에선 쓸모없다"(Asquith 2005) |
| '살아 있는 테마 수' | 뒤집힘(2개면 -3.01%) |
| 한국 외국인+기관 동반 5일 | **거꾸로**(0일 52번 → 5일 46번) |
| MACD 다이버전스 | 6개 설정 중 0개 승리 |

**미국에는 외국인·기관 순매수에 해당하는 자료가 없다.** 거래소가 세어 주는 한국과 다르다.
흉내 낸 값 여섯 가지를 다 재 봤고 하나도 갈리지 않았다. 그 칸은 비워 두는 것이 맞다.

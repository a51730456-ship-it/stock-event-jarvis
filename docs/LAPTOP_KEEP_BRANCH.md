# `laptop-keep-20260826` 가지 — 쓰지 않는다 (2026-08-27 확인)

## 한 줄

**거기 있던 것은 지금 앱에 이미 다 들어 있다. 가져올 것이 없다.**
지우지는 않는다(상하님 지시 — "만약에 그게 다른 문제와 연결되어 있으면
되살려야 될 수도 있잖나"). **연결만 안 한다.**

## 무엇이었나

2026-08-26 08:46에 "노트북에서 하던 작업 보관 — 온라인 갈래로 옮기기 전
안전 저장"으로 만든 가지다. 커밋 여섯 개가 들어 있다.

| 커밋 | 한 일 | 지금 master |
|---|---|---|
| `ded2e2b` | 일반 테마매매 점수·후보 화면 정리 | 이미 있음 |
| `8242a1f` | 설명 창 굴림 고침 | 이미 있음 (2026-08-26에 확인칸 방식으로 다시 지음) |
| `93dd282` | 설명을 카드 모양으로 | 이미 있음 |
| `3fcde2d` | 폰 뒤로가기 막기 | 이미 있음 (`back_nav.py`) |
| `babd953` | 폰에서 설명 줄이기 | 이미 있음 (1,993 → 636자로 더 줄임) |
| `5e6cfb8` | 보관용 저장(그림·기록) | — |

## 하나씩 확인한 것 (2026-08-27)

1. **시험 파일 3개** (`test_jarvis3_briefing_data/news/store.py`)
   지금 코드에 대고 돌려 봤다 — **7개 중 5개가 깨진다.** 옛 기준을 지키고
   있어서다: 선정 종목 4개(지금 6개) · 추가 검색 8개(지금 12개) · 옛 뉴스
   장치 · 3개월치 차트(지금 6개월). **그대로는 못 쓴다.**

2. **재 본 결과 기록** (`research/_out/*.txt`, `*.csv`)
   그것을 만든 스크립트가 master에 그대로 있다 —
   `research/us_crash_holding.py` · `research/us_crash_leaveout.py`.
   **다시 돌리면 나온다.**

3. **그 가지에만 있는 함수 다섯**
   `_render_briefing_home` · `_briefing_card_notes` · `_briefing_sparkline` ·
   `_render_briefing_manager` · `_briefing_news_html`
   전부 지금 이름이 다를 뿐 **같은 일을 하는 것이 master에 있다**
   (`_render_stock_briefing` · `_render_briefing_card` · `_briefing_chart` ·
   `_render_briefing_manage` · `_render_briefing_news`).

## 지금 상태

- **온라인 저장소에는 없다.** 이 노트북에만 있다.
- 온라인 앱은 `master`만 본다. 이 가지는 **어디에도 연결돼 있지 않다.**
- 그래서 그냥 두면 저절로 막힌 상태다. 아무것도 안 해도 된다.

## 하면 안 되는 것

- **master에 합치지 않는다**(merge·rebase·cherry-pick).
- 그 가지의 시험 파일을 master로 옮기지 않는다 — 지금 코드를 깨뜨린다.
- 되살릴 일이 생기면 **먼저 상하님께 여쭙는다.**

"""‘이 테마 기법에 대한 설명’ 단추와 그 내용.

자비스3(미국)·자비스4(한국)가 같이 쓴다. 한 곳만 고치면 두 화면이 함께 바뀐다.

여기 적힌 숫자는 지어낸 것이 아니라 2026-07-29에 실제로 잰 값이다.
2005-03 ~ 2026-06 지수 일봉(코스피 5,253일 · S&P500 5,357일)으로,
그날까지의 자료로만 신호를 만들고 20일 뒤 종가로 성적을 쟀다.
부트스트랩 1,500회로 우연인지도 확인했다. 숫자를 고칠 일이 생기면
반드시 다시 재고 나서 고친다.
"""

from __future__ import annotations

# 계산 결과나 문구를 바꾸면 이 숫자를 올리고, 페이지의 요구 리비전도 같이 올린다.
MODULE_REVISION = 2026072922

BUTTON_LABEL = "📘 이 테마 기법에 대한 설명"

# 단추는 눌림목 찾기 단추와 같은 옷을 입힌다(밝은 스카이 블루 바탕·주황 글씨).
# 다만 좌우로 늘리지 않고 글자 크기만큼만 차지하게 두고, 오른쪽 끝에 붙인다.
BUTTON_CSS = """
<style>
/* 오른쪽 끝으로 붙이기.
   스트림릿 컨테이너는 세로 방향 flex라 justify-content는 '세로'에 걸리고,
   align-items는 스트림릿이 start로 못박아 둬서 덮어써지지 않는다(2026-07-29 실측).
   그래서 자식에 margin-left:auto를 줘 밀어낸다 — 이건 정렬 규칙을 안 타고 확실히 먹는다. */
.st-key-jarvis_method_help { margin: .1rem 0 .35rem; }
.st-key-jarvis_method_help > div { margin-left: auto !important; width: auto !important; }
.st-key-jarvis_method_help [data-testid="stPopover"] { width: auto !important; }
div[class*="st-key-jarvis_method_help"] button {
    background: #cfe9ff !important;
    border: 1px solid #8ec9f5 !important;
    border-radius: .5rem !important;
    padding: .35rem .9rem !important;
    min-height: 0 !important;
    width: auto !important;
}
div[class*="st-key-jarvis_method_help"] button:hover {
    background: #b9dfff !important;
    border-color: #6db6ee !important;
}
div[class*="st-key-jarvis_method_help"] button p {
    color: #c15f3c !important;
    font-size: .95rem !important;
    font-weight: 800 !important;
    margin: 0 !important;
    white-space: nowrap !important;
}
/* 폰 전용 규칙은 여기 두지 않는다 — CLAUDE.md 12번 규칙에 따라 mobile_ui.py의
   폰 묶음 안에만 둔다. 밖으로 새면 태블릿·PC까지 바뀐다.
   (이 주석에 폰 미디어쿼리 문자열을 그대로 쓰면 '폰 규칙 묶음은 하나여야 한다'는
   테스트가 이 주석까지 세어 버린다 — 2026-07-29 실제로 걸렸다.) */
</style>
"""

_COMMON_TAIL = """
#### 믿어도 되는 것과, 조심할 것

**믿어도 되는 것 — 값 자체입니다.** 현재가, 등락률, 거래대금, 수급 금액, 차트.
전부 거래소나 공개 화면에서 그대로 받아온 원자료입니다.

**조심할 것 — 점수와 순위입니다.** 조건을 몇 개나 만족했는지 세어 100점으로
환산한 숫자이지, 앞으로 얼마나 오를지를 맞힌 값이 아닙니다.
점수가 높다는 것은 *"지금 여러 조건에 걸린다"*는 뜻이지
*"이 종목이 오른다"*는 뜻이 아닙니다.

**지어내지 않습니다.** 못 가져온 값은 '못 읽음'으로 둡니다. 0으로 채우지 않습니다.
빈칸은 고장이 아니라, 확인되지 않은 것을 확인된 것처럼 보이지 않으려는 것입니다.

---

##### 어떻게 쟀나

2005년 3월부터 2026년 6월까지 지수 일봉으로 쟀습니다
(코스피 5,253일 · S&P500 5,357일). 그날까지의 자료만으로 신호를 만들고,
20일 뒤 종가로 성적을 매겼습니다 — 앞을 훔쳐보지 않았습니다.
같은 자료를 1,500번 다시 뽑아(부트스트랩) 그 차이가 우연인지도 확인했습니다.

지수로 잰 이유가 있습니다. 종목을 골라 뽑으면 *오늘 잘나가는 종목만 모으는*
편향이 끼는데, 지수는 사라지지도 새로 생기지도 않아 그 함정이 없습니다.
"""

US_TEXT = f"""
### 이 화면은 무엇을 하는 도구인가

**"언제 사야 하나"를 찍어주는 도구가 아닙니다.
"지금 사면 얼마나 위험한가"를 재는 도구입니다.**

---

#### 시장 점수의 진짜 뜻

자비스는 S&P500·나스닥100이 50일선 위인지 아래인지를 시장 점수의 가장 큰 축으로
씁니다(100점 중 45점). 이 신호가 실제로 무엇을 맞히는지 21년치 지수로 직접 재봤습니다.

**더 벌게 해주지는 않습니다.**
S&P500이 50일선 **위**일 때 20일 뒤 평균 수익은 **+0.65%**,
**아래**일 때는 **+1.12%**였습니다. 오히려 눌렸을 때 산 쪽이 나았습니다.

**대신 크게 깨지는 것을 막아 줍니다.** 여기서 값어치가 나옵니다.

| 20일 뒤 | 50일선 **위** | 50일선 **아래** |
|---|---|---|
| 10% 넘게 깨질 확률 | **1.1%** | **3.5%** (3배) |
| 최악 1% 구간 손실 | **−10.4%** | **−19.4%** (2배) |
| 출렁임(표준편차) | **3.6%** | **6.0%** (1.7배) |

그래서 시장 점수는 이렇게 읽으십시오.

- 점수가 낮다 = **"지금 사면 더 크게 깨질 수 있다"** ← 맞는 말
- 점수가 낮다 = "지금 사면 덜 번다" ← **틀린 말**

**미국은 이 방어 장치가 잘 듣습니다.** 한국(자비스4)에서는 같은 신호가 훨씬 약합니다.

{_COMMON_TAIL}
"""

KR_TEXT = f"""
### 이 화면은 무엇을 하는 도구인가

**"언제 사야 하나"를 찍어주는 도구가 아닙니다.
"지금 사면 얼마나 위험한가"를 재는 도구입니다.**

---

#### 시장 점수의 진짜 뜻

자비스는 코스피·코스닥이 50일선 위인지 아래인지를 시장 점수의 큰 축으로
씁니다(100점 중 30점). 이 신호가 실제로 무엇을 맞히는지 21년치 코스피로 직접 재봤습니다.

**더 벌게 해주지는 않습니다.**
코스피가 50일선 **위**일 때 20일 뒤 평균 수익은 **+1.01%**,
**아래**일 때는 **+0.83%**였습니다. 차이가 우연과 구분되지 않았습니다.
중앙값과 승률로 보면 오히려 50일선 아래가 조금 나았습니다.

**깨짐을 막는 효과도 한국에서는 약합니다.**

| 20일 뒤 | 50일선 **위** | 50일선 **아래** |
|---|---|---|
| 10% 넘게 깨질 확률 | 2.9% | 3.6% |
| 최악 1% 구간 손실 | −15.2% | −15.5% |
| 출렁임(표준편차) | 6.1% | 6.1% |

미국은 같은 표에서 3배·2배·1.7배로 뚜렷하게 갈립니다. **한국은 거의 갈리지 않습니다.**

그래서 한국 화면에서는 이렇게 쓰십시오.

- 시장 점수는 **참고**로만 보십시오. 낮다고 무조건 손 놓을 근거가 못 됩니다
- 대신 **종목별 수급(외국인·기관)**을 더 무겁게 보십시오. 이건 한국에만 있는
  자료이고, 미국에는 아예 없는 무기입니다

- 점수가 높다 = "지금 여러 조건에 걸려 있다" ← 맞는 말
- 점수가 높다 = "이 종목이 오른다" ← **틀린 말**

{_COMMON_TAIL}
"""


def render(st, market: str) -> None:
    """최상단 오른쪽에 설명 단추를 놓는다. market은 'US' 또는 'KR'."""
    st.markdown(BUTTON_CSS, unsafe_allow_html=True)
    box = st.container(key="jarvis_method_help")
    with box:
        with st.popover(BUTTON_LABEL):
            st.markdown(US_TEXT if str(market).upper() == "US" else KR_TEXT)
